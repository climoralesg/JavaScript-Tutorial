let variable0 = 0;
let variable1 = 1;

const funcion = (a, b) => {
    console.log("imprimir pantalla dentro de la funcion");
    return a + b;
}

const ejecutarFuncion = funcion(variable0, variable1);

//console.log(funcion(variable0,variable1));
//console.log(ejecutarFuncion);