/*
Los generadores son funciones en las que se puede salir y volver a entrar
Su contexto (asociacion de variables) sera conservado entre las reentradas.

Cada vez que llamamos next, la ejecucion del generador va a continuar hasta el proximo tield

podemos hacer generadores infinitos

cuando llamamos next tambien podemos pasar valores que la funcion recibe 



*/

function* simpleGenerator(){

}

const get=simpleGenerator()